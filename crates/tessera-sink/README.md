# tessera-sink

Atomic-write worker pool to disk, built on [`tessera-pool`](../tessera-pool/) for
SHM zero-copy handoff between producer and worker subprocesses.

**Status**: v0.0.1 scaffold. Implementation lands in Stage 4c of the
upstream extraction plan.

See the [workspace README](../../README.md) for the planned design.
