# tessera-channel (Python)

Python facade for [`tessera-channel`](../../crates/tessera-channel/) — non-lossy MPSC shared-memory queue.

**Status**: v0.0.1 scaffold. Implementation lands in Stage 4c of the upstream extraction plan.

```python
# Future:
from tessera_channel import Channel
```
