"""Tessera Sink — atomic-write worker pool to disk.

v0.0.1 SCAFFOLD ONLY. The ``Sink`` class will land alongside the Rust
core implementation in Stage 4c (depends on tessera-pool from Stage 4a).
"""

__version__ = "0.0.1"
__all__: list[str] = []
